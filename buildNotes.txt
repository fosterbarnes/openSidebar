Fixes. v1.2.6

Settings
- Project-specific openSidebar settings now override user settings

Weekly Usage
- Tells you when to reconnect if usage is unavailable

Plugin
- Correctly named 'openSidebar' in OpenCode plugins menu

Maintenance
- Cleaned up duplicate work without changing behavior